import { Router } from "express";

const router = Router();


router.post('/login', (req, res) => {
    try{


    } catch(err){

    }
});


router.post('/create', (req, res) => {
    try{


    } catch(err){

    }
});

export default router;